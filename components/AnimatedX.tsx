"use client";

/**
 * Inline "X" for body mentions of MGX.
 * Renders the letter X in MGX brand green (#2CBF68) — flat, no animation.
 * Inherits font-size and font-weight from parent.
 */
export default function AnimatedX() {
  return (
    <span style={{ color: "#2CBF68" }}>X</span>
  );
}
